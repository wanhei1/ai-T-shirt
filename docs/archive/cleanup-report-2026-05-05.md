# Code Hygiene Cleanup Report — 2026-05-05

Generated by `npx ts-prune` and `npx depcheck`. **No code was auto-deleted.**

---

## 1. ts-prune — Unused Exports (frontend)

### Likely Dead Code (exported but never imported elsewhere)

| File | Export | Notes |
|------|--------|-------|
| `lib/comfyui-client.ts:274` | `SimpleComfyUIClient` | Duplicated by `lib/simple-comfyui-client.ts` |
| `lib/tshirt-workflows.ts:17` | `createTShirtGraphicWorkflow` | Check if used via dynamic import |
| `lib/tshirt-workflows.ts:115` | `createSDXLTShirtWorkflow` | Check if used via dynamic import |
| `lib/tshirt-workflows.ts:256` | `getTShirtStylePreset` | Check if used via dynamic import |
| `types/auth.ts:28` | `ApiError` | Unused — consider removing or importing |
| `components/auth/user-menu.tsx:18` | `UserMenu` | Check if used via barrel export |

### Module-Internal Exports (safe — re-exported within same file)

~250 exports flagged as "used in module" — these are shadcn/ui components and utility re-exports. They are **not dead code**; ts-prune flags them because they are exported but only used within their own module boundary. No action needed.

---

## 2. depcheck — Unused Dependencies

### frontend/

**Unused dependencies:**
- `autoprefixer` — likely a PostCSS config leftover
- `date-fns` — check if still used in any component
- `tailwindcss-animate` — check if referenced in tailwind.config

**Unused devDependencies:**
- `@tailwindcss/postcss`
- `@types/bun`
- `postcss`
- `tailwindcss`
- `tw-animate-css`

### backend/

No unused dependencies detected. ✅

---

## 3. Duplicate Types Between frontend/ and shared/

### Overlap: `frontend/types/auth.ts` ↔ `shared/src/types/index.ts`

| Type | frontend/types/auth.ts | shared/src/types/index.ts | Match? |
|------|----------------------|--------------------------|--------|
| `User` | has `is_admin?: boolean`, `created_at` is optional string | has `created_at: Date`, no `is_admin` | ⚠️ Different shapes |
| `AuthResponse` | `user: User` | `user: Omit<User, 'password'>` | ⚠️ Different |
| Login/Register | `LoginRequest`, `RegisterRequest` | `UserLogin`, `UserRegistration` | ⚠️ Same concept, different names |

**Recommendation:** Do NOT auto-migrate. The types have semantically different shapes. Manual review required before consolidation.

### Overlap: `frontend/types/design.ts` ↔ `shared/src/types/index.ts`

| Type | frontend/types/design.ts | shared/src/types/index.ts | Match? |
|------|-------------------------|--------------------------|--------|
| `DesignElement` | Full canvas element (x, y, width, height, rotation, etc.) | Simplified (id, type, content, position) | ⚠️ Different shapes |

**Recommendation:** The frontend version is more complete. Consider updating `shared/` to match the frontend version, then re-import. Manual review required.

### Backend → Shared

Backend only imports `API_COMMON_ERROR_CODES` and `REQUEST_VALIDATION_ERROR_CODES` from shared. No other type overlaps detected.

---

## 4. Other Observations

- `.copilot-tasks.json` is tracked but empty (0 bytes) — safe to remove from git
- `frontend-components-package-20251002-094615/` was already untracked in Phase 1
- `frontend` does not import from `shared/` at all — opportunity to consolidate auth/design types

---

## 5. Recommendations

1. **High priority:** Investigate `SimpleComfyUIClient` duplication in `lib/comfyui-client.ts` vs `lib/simple-comfyui-client.ts`
2. **Medium priority:** Manually review and consolidate `frontend/types/auth.ts` into `shared/src/types/`
3. **Low priority:** Remove unused `ApiError` export from `frontend/types/auth.ts`
4. **Low priority:** Audit `date-fns` usage in frontend — may be removable
5. **Informational:** ~250 shadcn/ui exports flagged by ts-prune are false positives (module-internal re-exports)
