下面是一份可以直接放进你项目里的 Markdown 部署文档，建议保存为：

```txt
DEPLOY_BIT810.md
```

这份文档按你的目标来写：**把项目部署到本地服务器，并最终通过 `www.bit810.cn` 访问**。里面同时包含了内测、备案、域名解析、Nginx、HTTPS、前后端部署、数据库配置和更新流程。

---

```markdown
# ai-T-shirt 项目上线部署文档：部署到 www.bit810.cn

本文档用于将 `ai-T-shirt` 项目部署到本地服务器，并最终通过域名：

```txt
https://www.bit810.cn
```

对外访问。

项目仓库：

```txt
https://github.com/wanhei1/ai-T-shirt
```

---

## 1. 部署目标

本项目采用前后端分离结构：

```txt
ai-T-shirt/
├── frontend/   # Next.js 前端
├── backend/    # Express 后端
├── shared/     # 共享代码
└── package.json
```

上线后整体架构如下：

```txt
用户浏览器
    ↓
https://www.bit810.cn
    ↓
Nginx
    ├── /       → Next.js 前端服务 localhost:3000
    └── /api    → Express 后端服务 localhost:3001
                      ↓
                 PostgreSQL 数据库 localhost:5432
```

---

## 2. 是否需要拆分 GitHub 仓库？

不需要。

当前项目可以继续保持一个 GitHub 仓库，也就是 Monorepo 结构：

```txt
frontend + backend + shared 放在同一个仓库
```

部署时只需要在服务器上分别进入：

```bash
frontend/
backend/
```

然后分别安装依赖、构建、启动即可。

---

## 3. 上线前置条件

正式通过 `www.bit810.cn` 对外访问前，需要满足以下条件。

### 3.1 本地服务器要求

推荐服务器系统：

```txt
Ubuntu 22.04 LTS / Ubuntu 24.04 LTS / Debian
```

推荐配置：

| 用途                  | 配置                |
| --------------------- | ------------------- |
| 内部测试              | 2 核 2G             |
| 小规模正式使用        | 2 核 4G             |
| 图片较多 / 访问量稍大 | 4 核 8G             |
| AI 生图               | 建议单独 GPU 服务器 |

---

### 3.2 网络要求

如果要让公网用户访问 `www.bit810.cn`，本地服务器需要满足：

1. 有公网 IP；
2. 路由器可以做端口映射；
3. 运营商没有封禁 `80` 和 `443` 端口；
4. 域名 `www.bit810.cn` 可以解析到该公网 IP；
5. 如服务器位于中国大陆，正式对外访问前需要完成 ICP 备案。

---

### 3.3 备案说明

如果服务器在中国大陆，并且使用：

```txt
www.bit810.cn
```

对外提供网站访问，通常需要 ICP 备案。

建议流程：

```txt
先用 IP / 局域网 / VPN 内测
备案通过后再正式解析 www.bit810.cn
```

不建议在未备案的情况下，将 `www.bit810.cn` 直接解析到中国大陆服务器并公开访问。

---

## 4. 本地服务器网络规划

假设本地服务器局域网 IP 为：

```txt
192.168.1.100
```

公网 IP 为：

```txt
你的公网IP
```

正式域名为：

```txt
www.bit810.cn
```

服务端口规划：

| 服务         | 地址           | 说明           |
| ------------ | -------------- | -------------- |
| Nginx        | 80 / 443       | 对外访问入口   |
| Next.js 前端 | 127.0.0.1:3000 | 只允许本机访问 |
| Express 后端 | 127.0.0.1:3001 | 只允许本机访问 |
| PostgreSQL   | 127.0.0.1:5432 | 不允许公网访问 |

---

## 5. 初始化服务器环境

以下命令默认在 Ubuntu / Debian 系统执行。

### 5.1 更新系统

```bash
sudo apt update
sudo apt upgrade -y
```

---

### 5.2 安装基础工具

```bash
sudo apt install -y git curl wget vim nginx ufw
```

---

### 5.3 安装 Node.js 20

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt install -y nodejs
```

检查版本：

```bash
node -v
npm -v
```

建议 Node.js 版本为：

```txt
v20.x
```

---

### 5.4 安装 PM2

PM2 用于守护前端和后端服务。

```bash
sudo npm install -g pm2
```

检查版本：

```bash
pm2 -v
```

---

## 6. 安装 PostgreSQL 数据库

### 6.1 安装 PostgreSQL

```bash
sudo apt install -y postgresql postgresql-contrib
```

启动数据库：

```bash
sudo systemctl enable postgresql
sudo systemctl start postgresql
```

检查状态：

```bash
sudo systemctl status postgresql
```

---

### 6.2 创建数据库和用户

进入 PostgreSQL：

```bash
sudo -u postgres psql
```

执行以下 SQL：

```sql
CREATE DATABASE aitshirt;
CREATE USER aitshirt_user WITH PASSWORD '请替换成你的强密码';
GRANT ALL PRIVILEGES ON DATABASE aitshirt TO aitshirt_user;

\c aitshirt

GRANT ALL ON SCHEMA public TO aitshirt_user;
ALTER SCHEMA public OWNER TO aitshirt_user;
```

退出 PostgreSQL：

```sql
\q
```

数据库连接字符串格式：

```env
DATABASE_URL=postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt
```

---

## 7. 拉取项目代码

### 7.1 创建项目目录

```bash
sudo mkdir -p /var/www
sudo chown -R $USER:$USER /var/www
cd /var/www
```

---

### 7.2 从 GitHub 拉取项目

```bash
git clone https://github.com/wanhei1/ai-T-shirt.git
cd ai-T-shirt
```

如果服务器访问 GitHub 很慢，可以先将 GitHub 仓库同步到 Gitee，再从 Gitee 拉取。

---

## 8. 部署后端 backend

### 8.1 进入后端目录

```bash
cd /var/www/ai-T-shirt/backend
```

---

### 8.2 安装依赖

```bash
npm install
```

---

### 8.3 创建后端环境变量

创建 `.env` 文件：

```bash
vim .env
```

内测阶段可以先写局域网地址：

```env
NODE_ENV=production
PORT=3001

DATABASE_URL=postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt

JWT_SECRET=请替换成一个很长很随机的字符串

FRONTEND_URL=http://192.168.1.100
CORS_ORIGIN=http://192.168.1.100
```

正式上线 `www.bit810.cn` 后，改成：

```env
NODE_ENV=production
PORT=3001

DATABASE_URL=postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt

JWT_SECRET=请替换成一个很长很随机的字符串

FRONTEND_URL=https://www.bit810.cn
CORS_ORIGIN=https://www.bit810.cn
```

注意：

```txt
JWT_SECRET 必须使用复杂随机字符串，不要使用 123456、test、secret 等简单值。
```

---

### 8.4 构建后端

```bash
npm run build
```

如果该命令报错，请检查：

```bash
cat package.json
```

确认实际脚本名称。

---

### 8.5 启动后端

```bash
pm2 start npm --name aitshirt-backend -- start
```

查看运行状态：

```bash
pm2 list
```

查看后端日志：

```bash
pm2 logs aitshirt-backend
```

---

## 9. 部署前端 frontend

### 9.1 进入前端目录

```bash
cd /var/www/ai-T-shirt/frontend
```

---

### 9.2 安装依赖

```bash
npm install
```

---

### 9.3 创建前端环境变量

创建 `.env.production` 文件：

```bash
vim .env.production
```

内测阶段：

```env
NEXT_PUBLIC_API_URL=http://192.168.1.100/api
```

正式上线后：

```env
NEXT_PUBLIC_API_URL=https://www.bit810.cn/api
```

注意：

```txt
Next.js 中 NEXT_PUBLIC_ 开头的变量会在构建时写入前端代码。
所以修改 .env.production 后，必须重新 npm run build。
```

---

### 9.4 构建前端

```bash
npm run build
```

---

### 9.5 启动前端

```bash
pm2 start npm --name aitshirt-frontend -- start
```

查看状态：

```bash
pm2 list
```

查看前端日志：

```bash
pm2 logs aitshirt-frontend
```

---

## 10. 配置 Nginx 反向代理

Nginx 用于统一入口：

```txt
/       → 前端 localhost:3000
/api    → 后端 localhost:3001
```

---

### 10.1 创建 Nginx 配置文件

```bash
sudo vim /etc/nginx/sites-available/aitshirt
```

写入以下内容：

```nginx
server {
    listen 80 default_server;
    server_name www.bit810.cn bit810.cn _;

    client_max_body_size 50m;

    location /api/ {
        proxy_pass http://127.0.0.1:3001/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

### 10.2 启用 Nginx 配置

```bash
sudo ln -s /etc/nginx/sites-available/aitshirt /etc/nginx/sites-enabled/aitshirt
sudo rm -f /etc/nginx/sites-enabled/default
```

检查配置：

```bash
sudo nginx -t
```

重启 Nginx：

```bash
sudo systemctl restart nginx
```

---

## 11. 内测阶段访问方式

在正式解析域名前，可以先通过局域网 IP 测试：

```txt
http://192.168.1.100
```

如果在本机服务器上测试：

```bash
curl http://127.0.0.1
```

如果后端有健康检查接口，可以测试：

```bash
curl http://127.0.0.1/api/health
```

或者：

```bash
curl http://192.168.1.100/api/health
```

具体路径以项目后端实际接口为准。

---

## 12. 给内测网站添加密码保护

如果网站还没备案，或者只是自己人测试，建议加 Nginx 访问密码。

### 12.1 安装密码工具

```bash
sudo apt install -y apache2-utils
```

---

### 12.2 创建访问账号

```bash
sudo htpasswd -c /etc/nginx/.htpasswd testuser
```

输入密码后，会生成访问账号：

```txt
testuser
```

---

### 12.3 修改 Nginx 配置

编辑：

```bash
sudo vim /etc/nginx/sites-available/aitshirt
```

在 `server` 块中添加：

```nginx
auth_basic "Private Test";
auth_basic_user_file /etc/nginx/.htpasswd;
```

示例：

```nginx
server {
    listen 80 default_server;
    server_name www.bit810.cn bit810.cn _;

    auth_basic "Private Test";
    auth_basic_user_file /etc/nginx/.htpasswd;

    client_max_body_size 50m;

    location /api/ {
        proxy_pass http://127.0.0.1:3001/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

检查并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 13. 配置防火墙

只开放必要端口：

```bash
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
```

检查状态：

```bash
sudo ufw status
```

不要对公网开放以下端口：

```txt
3000
3001
5432
```

原因：

| 端口 | 服务        | 是否应该公网开放 |
| ---- | ----------- | ---------------- |
| 3000 | Next.js     | 否               |
| 3001 | Express API | 否               |
| 5432 | PostgreSQL  | 绝对不要         |

---

## 14. 设置 PM2 开机自启

保存当前 PM2 进程：

```bash
pm2 save
```

生成开机启动命令：

```bash
pm2 startup
```

执行后，终端会输出一行命令，复制并执行它。

然后再次保存：

```bash
pm2 save
```

查看进程：

```bash
pm2 list
```

---

## 15. 路由器端口映射

如果本地服务器在内网，需要在路由器中配置端口转发。

假设本地服务器 IP 是：

```txt
192.168.1.100
```

需要映射：

| 公网端口 | 内网 IP       | 内网端口 | 用途  |
| -------- | ------------- | -------- | ----- |
| 80       | 192.168.1.100 | 80       | HTTP  |
| 443      | 192.168.1.100 | 443      | HTTPS |

配置完成后，可以在外网访问：

```txt
http://你的公网IP
```

如果运营商封禁 80 或 443，需要联系运营商或改用云服务器 / CDN / 反向代理方案。

---

## 16. 域名解析到本地服务器

备案通过，并确认公网访问正常后，在域名 DNS 控制台添加解析。

假设公网 IP 为：

```txt
你的公网IP
```

添加：

| 类型 | 主机记录 | 记录值     |
| ---- | -------- | ---------- |
| A    | www      | 你的公网IP |
| A    | @        | 你的公网IP |

解析完成后，访问：

```txt
http://www.bit810.cn
```

DNS 生效可能需要几分钟到数小时。

---

## 17. 配置 HTTPS 证书

正式上线建议开启 HTTPS。

### 17.1 安装 Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

---

### 17.2 申请证书

确保域名已经解析到当前服务器后，执行：

```bash
sudo certbot --nginx -d www.bit810.cn -d bit810.cn
```

根据提示选择自动跳转 HTTPS。

完成后访问：

```txt
https://www.bit810.cn
```

---

### 17.3 检查证书自动续期

```bash
sudo certbot renew --dry-run
```

如果没有报错，说明自动续期正常。

---

## 18. 正式上线后的环境变量确认

正式上线后，确认后端 `.env`：

```bash
cd /var/www/ai-T-shirt/backend
cat .env
```

应包含：

```env
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt
JWT_SECRET=你的长随机字符串
FRONTEND_URL=https://www.bit810.cn
CORS_ORIGIN=https://www.bit810.cn
```

确认前端 `.env.production`：

```bash
cd /var/www/ai-T-shirt/frontend
cat .env.production
```

应包含：

```env
NEXT_PUBLIC_API_URL=https://www.bit810.cn/api
```

修改前端环境变量后必须重新构建：

```bash
npm run build
pm2 restart aitshirt-frontend
```

修改后端环境变量后必须重启后端：

```bash
pm2 restart aitshirt-backend
```

---

## 19. 项目更新流程

以后代码更新后，在服务器执行：

```bash
cd /var/www/ai-T-shirt
git pull
```

---

### 19.1 更新后端

```bash
cd /var/www/ai-T-shirt/backend
npm install
npm run build
pm2 restart aitshirt-backend
```

查看日志：

```bash
pm2 logs aitshirt-backend
```

---

### 19.2 更新前端

```bash
cd /var/www/ai-T-shirt/frontend
npm install
npm run build
pm2 restart aitshirt-frontend
```

查看日志：

```bash
pm2 logs aitshirt-frontend
```

---

### 19.3 重载 Nginx

如果修改了 Nginx 配置：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 20. 常用排查命令

### 20.1 查看 PM2 服务

```bash
pm2 list
```

---

### 20.2 查看前端日志

```bash
pm2 logs aitshirt-frontend
```

---

### 20.3 查看后端日志

```bash
pm2 logs aitshirt-backend
```

---

### 20.4 查看 Nginx 状态

```bash
sudo systemctl status nginx
```

---

### 20.5 查看 Nginx 错误日志

```bash
sudo tail -f /var/log/nginx/error.log
```

---

### 20.6 查看 Nginx 访问日志

```bash
sudo tail -f /var/log/nginx/access.log
```

---

### 20.7 查看 PostgreSQL 状态

```bash
sudo systemctl status postgresql
```

---

### 20.8 测试本机端口

```bash
curl http://127.0.0.1:3000
curl http://127.0.0.1:3001
curl http://127.0.0.1
```

---

### 20.9 查看端口占用

```bash
sudo ss -tulpn
```

重点确认：

```txt
80    nginx
3000  node / next
3001  node / express
5432  postgres
```

---

## 21. 常见问题

### 21.1 访问 www.bit810.cn 打不开

检查顺序：

1. 域名是否解析到正确公网 IP；
2. 路由器是否映射 80 / 443；
3. 服务器防火墙是否开放 80 / 443；
4. Nginx 是否正常运行；
5. 运营商是否封禁 80 / 443；
6. 是否已经完成备案。

排查命令：

```bash
sudo systemctl status nginx
sudo ufw status
curl http://127.0.0.1
```

---

### 21.2 页面能打开，但接口请求失败

检查：

1. 前端 `.env.production` 是否正确；
2. 是否重新执行了 `npm run build`；
3. Nginx `/api/` 代理是否正确；
4. 后端是否运行；
5. 后端 CORS 是否允许 `https://www.bit810.cn`。

查看后端日志：

```bash
pm2 logs aitshirt-backend
```

---

### 21.3 修改 NEXT_PUBLIC_API_URL 后不生效

原因：

```txt
Next.js 的 NEXT_PUBLIC_ 变量是在构建时写入的。
```

解决：

```bash
cd /var/www/ai-T-shirt/frontend
npm run build
pm2 restart aitshirt-frontend
```

---

### 21.4 数据库连接失败

检查：

```bash
sudo systemctl status postgresql
```

测试数据库登录：

```bash
psql "postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt"
```

如果权限错误，进入 PostgreSQL 重新授权：

```bash
sudo -u postgres psql
```

执行：

```sql
\c aitshirt
GRANT ALL ON SCHEMA public TO aitshirt_user;
ALTER SCHEMA public OWNER TO aitshirt_user;
```

---

### 21.5 证书申请失败

检查：

1. `www.bit810.cn` 是否已经解析到当前公网 IP；
2. 80 端口是否能从公网访问；
3. Nginx 是否运行；
4. 是否有其他服务占用 80 端口。

命令：

```bash
sudo nginx -t
sudo systemctl status nginx
sudo ss -tulpn | grep :80
```

---

## 22. 安全建议

正式上线后建议：

1. 不要开放 `3000`、`3001`、`5432` 到公网；
2. PostgreSQL 只监听本机；
3. 使用强密码；
4. 定期备份数据库；
5. 定期更新系统；
6. Nginx 配置 HTTPS；
7. 后台接口增加鉴权；
8. 图片上传限制文件大小和文件类型；
9. 不要把 `.env` 提交到 GitHub；
10. 不要在代码中写死数据库密码和 JWT_SECRET。

---

## 23. 数据库备份

### 23.1 手动备份

```bash
pg_dump "postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt" > aitshirt_backup.sql
```

---

### 23.2 恢复备份

```bash
psql "postgresql://aitshirt_user:你的强密码@localhost:5432/aitshirt" < aitshirt_backup.sql
```

---

## 24. 推荐上线顺序

建议严格按以下顺序执行：

```txt
1. 本地服务器安装 Node.js、Nginx、PM2、PostgreSQL
2. 拉取 GitHub 项目
3. 配置 PostgreSQL 数据库
4. 部署 backend 到 3001
5. 部署 frontend 到 3000
6. 配置 Nginx 代理 / 和 /api
7. 局域网 IP 测试
8. 配置路由器端口映射
9. 公网 IP 测试
10. 完成 ICP 备案
11. 解析 www.bit810.cn 到公网 IP
12. 配置 HTTPS
13. 正式访问 https://www.bit810.cn
```

---

## 25. 最终访问地址

内测阶段：

```txt
http://192.168.1.100
```

公网 IP 测试阶段：

```txt
http://你的公网IP
```

正式上线阶段：

```txt
https://www.bit810.cn
```

---

## 26. 最终部署检查清单

上线前确认：

- [ ] PostgreSQL 正常运行
- [ ] 后端 `aitshirt-backend` 正常运行
- [ ] 前端 `aitshirt-frontend` 正常运行
- [ ] Nginx 正常运行
- [ ] `/api` 可以访问后端
- [ ] 前端页面可以打开
- [ ] 登录 / 注册 / 订单功能正常
- [ ] 服务器防火墙只开放 22、80、443
- [ ] 路由器已映射 80、443
- [ ] 域名已备案
- [ ] `www.bit810.cn` 已解析到公网 IP
- [ ] HTTPS 证书已配置
- [ ] `.env` 没有提交到 GitHub
- [ ] 数据库已做好备份策略

---

## 27. 一句话总结

当前项目不需要拆分 GitHub 仓库。

推荐部署方式是：

```txt
本地服务器 + Nginx + PM2 + PostgreSQL
```

服务结构是：

```txt
www.bit810.cn
    ↓
Nginx
    ├── /    → frontend:3000
    └── /api → backend:3001
```

最终正式访问地址：

```txt
https://www.bit810.cn
```

```
---

## 使用方式

把上面的内容保存到项目根目录：

```bash
cd /var/www/ai-T-shirt
vim DEPLOY_BIT810.md
```

然后粘贴进去保存即可。

或者你在本地项目里新建：

```txt
DEPLOY_BIT810.md
```

提交到 GitHub：

```bash
git add DEPLOY_BIT810.md
git commit -m "docs: add deployment guide for bit810.cn"
git push
```

这份文档可以作为你之后部署、排查、迁移服务器时的标准流程。